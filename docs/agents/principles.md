# 基本原则与沉淀

> 入口 `AGENTS.md:1`，本文为细节。

- 先读现有代码，再动手修改，优先沿用项目已有结构和写法。
- 最少行数，能简单实现不引入复杂抽象。
- 通用能力（格式/协议/解析/压缩/加密/日期）用成熟库，不手写底层。
- 不为兼容多场景写大量分支，只实现当前明确需求。
- 未上线不兼容旧数据，存储结构直接按新设计改。
- 写完不检查语法/构建，用户自做。
- 不改无关文件，不顺手重构。
- 工作区已有改动不回滚覆盖。

**反复提醒沉淀**：反复出现的问题补充为可执行规则，放最相关章节，找不到放 `project-notes.md:1`。
